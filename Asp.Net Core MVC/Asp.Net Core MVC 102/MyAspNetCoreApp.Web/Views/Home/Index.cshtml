@{
    ViewData["Title"] = "Home Page";

    var alert = "Bu bir uyarı mesajıdır";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Learn about <a href="https://learn.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
</div>

// HTML CSS JAVASCRIPT

<div>@DateTime.Now.ToShortDateString()</div>

@{
    var datetime = DateTime.Now.ToShortTimeString();
    var message = "Hello";

    @: Bugünün tarihi @* Razor syntaxdan çıkmak için:  @: *@
    @datetime

    //veya

    <hr />

    <text>Bugünün tarihi : @datetime </text>



    <div>Mesaj: @message</div>
}

@if (1 == 1) {
    <p>Doğru</p>
} else if (DateTime.IsLeapYear(DateTime.Now.Year)) {
    @datetime
} else {
    <p>Yanlış</p>
}

@foreach (var it in Enumerable.Range(1, 15)) {
    <p>@it</p>
}
